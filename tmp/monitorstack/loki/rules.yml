groups:
  - name: UID2
    rules:
      - alert: Test.Alert
        annotations:
          summary: 'Test alert'
        expr: 'sum(count_over_time({job="dockerlogs"} |= `ERROR` [1m])) > 0'
        labels:
          severity: error
          send_resolved: false
